name: graft_samples
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.8
  - pplacer  # Provides guppy and phyloxml_converter
  - biopython
  - pandas
  - pip
  - pip:
    - dendropy  # Additional tree manipulation library
    - phylotree  # Tree format conversion utilities